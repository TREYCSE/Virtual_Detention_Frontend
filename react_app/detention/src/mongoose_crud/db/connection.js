/*
import mongoose from "mongoose"; 
import dotenv from 'dotenv'
dotenv.config()
mongoose.connect(process.env.MONGODB_URL || '', { useNewUrlParser: true});
const db = mongoose.connection
db.on('error', err => console.error(err))
db.once('open', () => console.log('Connected to database'))

export default mongoose
*/
//save and store as JSON then seed into mongo? - check in with Paresh
//mongosh "mongodb+srv://cluster0.kovjjgf.mongodb.net" --apiVersion 1 --username treychelleweathers2017
//mongodb+srv://treychelleweathers2017:<password>@cluster0.kovjjgf.mongodb.net/